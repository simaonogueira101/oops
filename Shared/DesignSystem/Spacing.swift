import SwiftUI

/// The app's spacing scale (4-pt based). Use these tokens instead of magic numbers so
/// spacing stays consistent inside and between elements — enforced by SwiftLint.
enum Spacing {
    static let xxs: CGFloat = 4
    static let xs: CGFloat = 8
    static let sm: CGFloat = 12
    static let md: CGFloat = 16
    static let lg: CGFloat = 24
    static let xl: CGFloat = 32
}
